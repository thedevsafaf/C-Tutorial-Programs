#include <stdio.h> 
int main() 
{  
    printf("Hello World"); 
    return 0;
} 

/*

    * stdio.h is a C library.

    * printf() is a system function called from stdio.h library to display something written as statements / values.

    * main() is a system function where the program starts execution.

    * we use int / void return data type before main().
        
            if the function returns something, then we use int main()...
            if the function does not return, then we use void main()...

    * return 0 is indicated for the successful execution.


*/
