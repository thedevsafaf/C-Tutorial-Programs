#include <stdio.h>
int main(){
    int i;
    printf("ASCII Codes");
    for(i=0;i<256;i++){
        printf("%d -> %c\n",i,i);
    }
    return(0);
}

/*

Just think if we need to declare or display more than 1000 or 10000 variables in a single program.

Here the concept of Loop comes.

Loops can execute a block of code a number of times.

a type of Loop used in this pgm is for loop...(ll be discussed later).

*/
